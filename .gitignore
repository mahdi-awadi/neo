node_modules/
.env
config.json
*.db
*.sqlite
*.log
data/
dist/
# Runtime company/tenant data + secrets live here once ported — never committed.
company/

# The default project ("the company") — runtime config, memory, secrets
/agent/
/.superpowers/

# Operator attachments (photos, customer files, keys) — never committed
inbox/

# OS / editor cruft
.DS_Store
*.swp

# Machine-local — per-deployment files that must never dirty the tracked tree
CLAUDE.local.md
AGENTS.local.md
.claude/settings.local.json
.gitnexus/
.claude/skills/gitnexus/
